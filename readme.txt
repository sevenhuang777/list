1、题目一
任意输入3个整数，按从小到大的顺序组成一个单链表。
input1：输入insert xx 操作命令，插入指定的xx数值，插入后保持链表从小到大的顺序不变，完成后将链表打印出来，
格式如1 -> 2 -> 3 -> 4 -> 5 -> NULL

input2：输入delete xx操作命令，删除指定的xx数值，完成后将链表打印出来，格式同上。

要求：链表最多只允许有10个整数，超过后提示插入失败。
      
input1与input2的执行顺序无要求，满足任意执行多次。
      
用户界面需要给出可执行哪些命令的提示。